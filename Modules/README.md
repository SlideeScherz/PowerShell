# Modules

Place versioned PowerShell modules here.

Each module folder should contain:
- `<ModuleName>.psd1` (manifest)
- `<ModuleName>.psm1` (module)
- `Public/` and `Private/` function folders
- `Tests/` (Pester)

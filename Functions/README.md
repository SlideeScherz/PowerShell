# Functions

Reusable functions that are not yet packaged into a module.

- `Public/` — safe for dot-sourcing/export
- `Private/` — helper functions (do not export)

Tip: if a function becomes widely used, promote it into a proper module under `modules/`.
